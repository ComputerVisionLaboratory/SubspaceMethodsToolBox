classdef TestDataUsage
    enumeration
        SingleClass,     % Test the model on a single test data entry
        Subsets,          % Use subsets of the testing data
        WholeData     % Use the whole test data
    end
end
